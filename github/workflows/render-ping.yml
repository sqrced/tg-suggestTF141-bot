name: Keep Render Bot Awake

on:
  schedule:
    - cron: "*/5 * * * *"   # Запускается каждые 5 минут
  workflow_dispatch:        # Можно запустить вручную из вкладки Actions

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render bot
        run: |
          echo "Пингую Render бота..."
          curl -s -o /dev/null -w "%{http_code}\n" https://tg-suggesttf141-bot-6.onrender.com
